# MUCAR 硕士论文总体工作流

## 1. 工作目标

本工作流用于将《技术大纲》落地为一条可由 Codex 分阶段执行、可依据实验结果调整、且全过程可复现的研究路线。研究围绕“不完整遥测下的低轨卫星链路流量预测、不确定性校准与置信度感知路由”展开。

执行时必须遵循以下原则：

- 每一步只消费已经通过上一阶段验收的产物；
- 每一步均产生配置、数据清单、测试或结果报告，禁止仅保留临时脚本；
- 失败时返回产生问题的最早阶段，不通过下游调参掩盖数据或环境错误；
- 所有随机实验固定并公开种子，正式结果使用 5 个独立种子；
- 任何论文主张必须能追溯到数据、配置、代码版本和实验结果；
- 未达到成功判据时，允许缩减论文主张，不得选择性隐藏失败结果。

## 2. 阶段总览

| 步骤 | 阶段 | 主要产物 | 进入下一步的门槛 |
|---|---|---|---|
| 1 | 数据源落地与数据合同冻结 | 原始数据、哈希清单、许可记录、数据字典、标准化方案 | 全部来源可追溯，结构审计和复现测试通过 |
| 2 | 研究与仿真契约冻结 | 单位、时隙、Walker 参数、容量/缓存、随机种子、数据划分配置 | 所有参数有定义、单位和依据，无待定项 |
| 3 | 最小动态星座仿真器 | 轨道、覆盖、计划拓扑、运行拓扑、四邻居 ISL | 66 星轨迹和拓扑测试通过，固定种子可复现 |
| 4 | OD 需求生成器 | 时间强度、人口—时区空间权重、基础 OD、突发与漂移 | 全局总量守恒，空间权重归一，干预顺序正确 |
| 5 | 流级网络环境 | commodity 聚合、队列服务、时延、丢包、故障逻辑 | 业务守恒、缓存边界和故障行为测试通过 |
| 6 | 多行为策略数据生成 | Dijkstra、ECMP、Queue-aware 轨迹及四张逻辑表 | 三策略均可运行，链路负载可由明细还原 |
| 7 | 遥测缺失管线 | MCAR、MAR、Block Missing、掩码和信息年龄 | 缺失只改变观测，不改变完整真值 |
| 8 | 数据质量与防泄漏审计 | 守恒、主外键、干预顺序、时间划分、复现报告 | 所有硬性审计通过后冻结数据集版本 |
| 9 | 预测基线 | Historical Average、LSTM、GCN-GRU 及统一评估器 | 指标和数据接口一致，基线结果可复现 |
| 10 | MUCAR 预测模块 | Mask-aware GAT-GRU-D、单调分位数头 | 完整/缺失/跨策略实验完成，分位数交叉率为 0 |
| 11 | 不确定性校准 | 固定 CQR、延迟标签感知滑动 CQR、因果审计 | 无未来标签泄漏，分层覆盖结果完整 |
| 12 | 路由基线与 MAPPO 环境 | Dijkstra、ECMP、MAPPO、Point-MAPPO | 动作屏蔽、无环、等待动作和公平预算测试通过 |
| 13 | MUCAR 路由 | 置信门控、风险分数、区间输入、切换滞回 | 奖励仅使用实际结果，冻结预测器接口正确 |
| 14 | 正式实验 | 数据、预测、校准、路由、压力、消融、开销结果 | 5 种子运行完成，失败运行和配置全部公开 |
| 15 | 统计验证与结果分支 | 置信区间、效应量、配对检验、多重校正 | 统计假设检查完成，结论不超出证据 |
| 16 | 论文写作与完整性审计 | 论文、图表、主张—证据表、引用与数据审计 | 全部主张可追溯，最终复核通过 |

## 3. 阶段间数据流

```text
公开数据与固定配置
  → 地面 OD 与动态星座
  → 多策略完整网络真值
  → 不完整遥测观测
  → 缺失感知预测与预测区间
  → 冻结预测器驱动的多智能体路由
  → 压力、消融和资源实验
  → 统计验证与论文主张
```

## 4. 结果驱动的决策规则

### 4.1 数据与仿真阶段

- 数据守恒、主外键或干预顺序失败：回退到步骤 4–7 中最早产生错误的模块。
- 三种行为策略产生几乎相同的链路分布：先检查拓扑、分流和队列实现；确认实现正确后再调整 Queue-aware 温度和代价权重。
- 仿真负载长期无排队或长期全网饱和：在步骤 2 重新标定容量缩放，不修改预测模型掩盖问题。

### 4.2 预测与校准阶段

- 本文模型在完整数据上明显弱于 GCN-GRU：先检查输入/标签对齐与训练预算；仍失败则简化模型并缩减创新主张。
- 加入路由状态没有改善跨策略泛化：检查状态是否真实反映下一跳和分流；若信息正确仍无效，则将其作为否定结果报告。
- MAR 下 Observed-label 覆盖合格但 Full/High-load 覆盖不足：明确报告选择偏差，不宣称总体覆盖保证。
- 因果校准审计发现未来标签或插补标签进入窗口：该轮校准与路由结果全部作废，修复步骤 11 后重跑。

### 4.3 路由阶段

- MUCAR 正常场景明显退化：优先检查门控、状态尺度和切换振荡，而不是改变奖励以直接奖励预测风险。
- 压力场景不优于 Point-MAPPO：分别消融区间、信息年龄、风险分数和滞回，定位无效接口。
- 出现环路或选择故障链路：视为环境正确性失败，所有路由性能结果作废并返回步骤 12。
- M4 Pro 资源不足：按网络宽度、并行环境数、序列长度的顺序缩减；保持数据、基线预算和评价口径不变。

## 5. 统一工程规范

- 配置文件是实验事实的唯一来源，禁止在训练脚本中散落关键常数。
- 原始数据只读保存；所有派生数据记录输入哈希、配置哈希和代码提交号。
- 训练、验证、校准和测试严格按时间划分，窗口不得跨分区。
- `policy_id` 只用于分层、采样、划分和报告，不进入预测器前向计算。
- 预测器在路由训练期间冻结；所有学习型路由共享训练预算、actor/critic 容量和奖励定义。
- 每次正式运行保存标准输出、错误输出、耗时、峰值内存、退出码和结果清单。

## 6. 最低交付范围

最低版本必须包含：66 星 Walker 仿真器、Abilene 与人口—时区需求映射、三种行为策略、四类缺失/故障、Mask-aware GAT-GRU-D、Quantile Regression 与滑动 CQR、四种 MAPPO 路由变体、六类核心实验及 5 个随机种子。GEANT、真实 TLE 和更大规模星座均属于可选扩展，不得阻塞最低版本。
