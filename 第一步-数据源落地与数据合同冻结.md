# 第一步：数据源落地与数据合同冻结

## 1. 目标与边界

本步骤只确认原始数据能否合法、稳定、可复现地取得，字段是否足以构建外生 OD，以及后续实现是否可以在不重新选择数据集的情况下继续。

本步骤不实现卫星仿真、需求注入、路由、遥测缺失或神经网络。

## 2. 固定数据源

### 2.1 时间变化模式

- 主源：SNDlib Abilene 动态流量矩阵；
- 格式：native ASCII TGZ；
- 预期结构：12 节点、132 个非自环有向 OD；
- 时间结构：5 分钟粒度、6 个月、48,096 个矩阵；
- 用途：只提取全局业务强度和 OD 相对变化，不把 12 个路由器映射为 66 颗卫星；
- 官方页：<https://sndlib.put.poznan.pl/abilene.overview.action>；
- 回退源：GEANT 仅登记，不在本步骤自动启用。

### 2.2 空间人口权重

- 主源：WorldPop Global 2 Population Counts；
- 固定年份：2025；
- 目标分辨率：1 km；
- 格式：GeoTIFF；
- 用途：只生成空间权重，不改变每个时刻的全局业务总量；
- 官方目录：<https://www.worldpop.org/datacatalog/>。

若官方仅提供更高分辨率产品，允许确定性聚合至 1 km，但必须记录聚合窗口、NoData 规则、坐标系和质量统计。

### 2.3 当地时间

- 使用 IANA 时区边界的固定发布版本；
- 下载后本地空间连接，不调用在线时区接口；
- manifest 必须记录发布标签、资产 URL、许可和 SHA-256；
- 跨换日线几何必须有专门测试。

### 2.4 轨道数据

本研究不下载商业星座 TLE。后续使用固定 Walker 66/6/2 参数生成理想化轨道；本步骤只在配置中登记这一事实。

## 3. 目录和版本控制

```text
data/
  raw/                 # 原始压缩包和栅格，只读、不入 Git
  interim/             # 解包与中间解析结果，不入 Git
  processed/           # 标准化全量数据，不入 Git
  manifests/           # 来源、哈希、许可、处理记录，进入 Git
configs/
  data_sources.yaml
docs/
  data_source_report.md
  data_dictionary.md
scripts/
  download_data.py
  inspect_raw_data.py
  build_data_manifest.py
tests/
  data/                 # 小型测试夹具，进入 Git
```

下载必须先写入同目录临时文件，完成大小和哈希检查后再原子重命名。重复运行发现目标文件与 manifest 哈希一致时直接复用，不得覆盖。

## 4. 数据源配置合同

`configs/data_sources.yaml` 至少包含：

```yaml
abilene:
  role: temporal_od_pattern
  source_page: https://sndlib.put.poznan.pl/abilene.overview.action
  format: sndlib_native_ascii
  expected_interval_minutes: 5
  expected_matrix_count: 48096
  expected_node_count: 12
  expected_directed_od_count: 132

worldpop:
  role: spatial_population_weight
  product: Global_2_population_counts
  resolution: 1km
  reference_year: 2025
  format: geotiff

timezone_boundaries:
  role: local_time_mapping
  format: geospatial_vector
  version: pinned_release

orbit:
  role: synthetic_walker_constellation
  external_download: false
  satellites: 66
  planes: 6
  phasing: 2
```

禁止在配置中只保存 `latest`。解析官方页面后，必须把最终下载 URL、发布日期、文件大小和 SHA-256 写入 manifest。

## 5. Codex 执行清单

### 5.1 环境预检

1. 记录 Python 和操作系统版本、可用磁盘空间和执行时间。
2. 检查 YAML、NumPy、GeoTIFF 和矢量空间数据依赖。
3. 分别对三个官方端点执行可达性检查。
4. 将结果写入环境报告；依赖缺失可以安装，许可不明不得自动放行。

### 5.2 Abilene 下载和解析

1. 从官方概览页解析 native TGZ 链接，不在代码中猜测镜像地址。
2. 流式下载至临时文件，记录最终 URL、响应时间、响应头和字节数。
3. 计算 SHA-256 后原子移动到 `data/raw/abilene/`。
4. 解包至 `data/interim/abilene/`，保存原始压缩包且不修改。
5. 解析所有矩阵并验证：
   - 矩阵数为 48,096；
   - 节点集合全程一致且为 12 个；
   - 每个矩阵包含 132 个非自环有向 OD；
   - 所有需求有限且非负；
   - 时间索引严格递增；若元数据没有可信日历时间，只使用 5 分钟序号。
6. 输出长表字段：

```text
timestamp_index, src_id, dst_id, demand_raw,
total_demand, global_intensity, od_share
```

7. `global_intensity` 的分母只使用训练段总需求中位数。时间划分按 60%/15%/10%/15% 向下取整确定边界，尾部余量归测试集。
8. 当某时刻总需求为零时，`global_intensity=0`，所有 `od_share=0`，并在审计报告中记录零总量时刻数。

### 5.3 WorldPop 处理

1. 从官方目录确定 2025 年 Population Counts 的固定资产 URL和产品版本。
2. 下载并记录 SHA-256、许可、CRS、变换矩阵、分辨率、NoData 和数据范围。
3. 使用窗口化读取，禁止将全球高分辨率栅格一次性载入内存。
4. 将负值、非有限值和 NoData 视为无效像元；保留无效掩码，人口权重置零。
5. 若需聚合到 1 km，人口计数使用求和重采样，不得使用均值。
6. 验证有效人口均非负、经纬度覆盖合理、聚合前后人口总量在容差内守恒。

### 5.4 时区映射和区域聚合

1. 下载固定版本的 IANA 时区边界并校验哈希。
2. 统一人口像元与时区边界的 CRS。
3. 使用像元中心点进行空间连接；跨换日线前将经度表示统一为 `[-180, 180)`。
4. 未命中的有效人口像元使用最近有效时区，但必须记录像元数、人口量和最大匹配距离。
5. 将人口格网聚合为：

```text
region_id, centroid_lat, centroid_lon,
population, timezone, population_weight
```

6. `population_weight = population / 全部区域人口总量`，并验证误差小于 `1e-8`。

## 6. Manifest 合同

每个外部资产的 manifest 必须至少包含：

```text
dataset_id, role, source_page, requested_url, final_url,
retrieved_at_utc, release_version, license, citation,
filename, byte_size, sha256, media_type,
processing_config_sha256, generated_outputs
```

每个 processed 文件必须反向引用全部原始输入哈希和处理配置哈希。

## 7. 测试夹具与自动测试

创建可提交的小型夹具：连续 3 个 Abilene 时间点、4 个 OD、跨至少两个时区的人口像元，并包含一个 NoData 像元。

自动测试必须覆盖：

1. 已存在且哈希一致的下载不被覆盖；
2. 中断下载只留下临时文件，不生成正式目标；
3. Abilene 数量、节点、OD、非负性和时间顺序校验；
4. 零总需求时刻的安全归一化；
5. 人口权重非负且总和误差小于 `1e-8`；
6. NoData 不进入人口总量；
7. 换日线两侧时区映射正确；
8. 固定输入重复处理得到字节级一致或规范化后哈希一致的产物；
9. processed 文件可追溯到输入与配置；
10. 处理前后原始文件哈希不变。

## 8. 失败分支

| 异常 | 强制处理 |
|---|---|
| Abilene 官方下载不可用 | 仅作网络退避重试；失败后停止，不自动换 GEANT |
| 矩阵少于 48,096 或结构异常 | 保留原件和审计报告，停止进入第二步 |
| 缺少可信日历时间 | 使用严格递增的 5 分钟索引，不虚构日期 |
| WorldPop 无直接 1 km 产品 | 从固定高分辨率人口计数确定性求和聚合 |
| 许可无法确认 | 只允许本地可行性检查，不得进入正式实验 |
| 全球栅格超出内存 | 分块/窗口化处理，不得随机抽样或缩小研究区域 |
| 人口或时区审计失败 | 修复后重跑，禁止以均匀人口权重替代 |

## 9. 输出文档

`docs/data_source_report.md` 记录每个来源的用途、版本、下载时间、许可、引用、哈希、结构统计、清洗规则、局限和完整复现命令。

`docs/data_dictionary.md` 对每个原始及标准字段记录：类型、单位、是否允许缺失、合法范围、来源字段、处理方法和下游使用者。

## 10. 完成门槛

只有以下条件全部满足，才能开始第二步：

- Abilene、WorldPop 和时区边界均固定版本、来源、许可与哈希；
- Abilene 全量结构审计通过；
- 人口守恒、权重和时区映射审计通过；
- 来源报告、数据字典、配置和复现命令齐全；
- 测试夹具及全部自动测试通过；
- 删除本地产物后，从空 `data/` 目录重新执行可生成规范化哈希一致的结果。
